var lengthOfLongestSubstring = function(s) {
  int max = 1;
  for(let i=2; i<s.length; i++ ) {
    for(let j=0; j<=s.length-i; j++ ) {
      let s1 = s.slice(j, j+2)
      for(let k=0; k<s1.length; k++){
        
      }
    }
  }

};